<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.broadcast_types">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Broadcasttypes">
        <activity
                android:name=".MainActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <receiver android:name=".ReceiverA" android:exported="false">
            <intent-filter android:priority="10" >
                <action android:name="my.same.named.receiver.action"/>
            </intent-filter>
        </receiver>
        <receiver android:name=".ReceiverB" android:exported="false">
            <intent-filter android:priority="30" >
                <action android:name="my.same.named.receiver.action"/>
            </intent-filter>
        </receiver>
        <receiver android:name=".ReceiverC" android:exported="false">
            <intent-filter android:priority="20" >
                <action android:name="my.same.named.receiver.action"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>